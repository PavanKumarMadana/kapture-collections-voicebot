# Optional. In local development, Vite proxies /webhook and /health to http://localhost:3000.
# In production, set this to the deployed mock backend origin if it is not same-origin.
VITE_API_BASE_URL=
